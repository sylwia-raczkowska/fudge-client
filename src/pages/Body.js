import { Switch } from 'react-router-dom'
import React from 'react';

const Body = () => (
    <main>
        <Switch>
            {/*<Route path='/register' component={Register}/>*/}
        </Switch>
    </main>
);

export default Body;